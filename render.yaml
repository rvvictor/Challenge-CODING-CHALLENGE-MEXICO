services:
  - type: web
    name: aurelion
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && npm --prefix frontend ci && npm --prefix frontend run build
    startCommand: python -m backend.app.main
    envVars:
      - key: MARKET_MODE
        value: demo
      - key: AUTO_EXECUTION
        value: "true"
